-- This is used to establish the data base that this site is going to use. The Drop database makes it so there is no duplication of information when starting the database over and over. 
DROP DATABASE IF EXISTS tech_blog_db;

CREATE DATABASE tech_blog_db;
